const METEOR_SUPPORTED_PACKAGES_IDENTIFIER = {
    VALIDATED_METHODS: {
        NAME: "ValidatedMethod",
        KEY: "name",
    },
    PUBLISH_COMPOSITE: "publishComposite",
};

const METEOR_IDENTIFIERS = {
    METEOR: "Meteor",
    METHODS: "methods",
    PUBLISH: "publish",
};

const TEMPLATE_CALLERS = {
    HELPERS: "helpers",
    ON_CREATED: "onCreated",
    ON_RENDERED: "onRendered",
};

const TAG_NAMES = {
    TEMPLATE: "template",
};

const FILE_EXTENSIONS = {
    HTML: ".html",
    JS: ".js",
    TS: ".ts",
};

module.exports = {
    METEOR_SUPPORTED_PACKAGES_IDENTIFIER,
    METEOR_IDENTIFIERS,
    TEMPLATE_CALLERS,
    TAG_NAMES,
    FILE_EXTENSIONS,
};
